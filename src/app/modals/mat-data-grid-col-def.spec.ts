import { MatDataGridColDef } from './mat-data-grid-col-def';

describe('MatDataGridColDef', () => {
  it('should create an instance', () => {
    expect(new MatDataGridColDef()).toBeTruthy();
  });
});
